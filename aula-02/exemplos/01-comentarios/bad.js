// 🧠 EXEMPLO: Comentário desnecessário
//
// Esse é um exemplo de MAU uso de comentários.
// O comentário repete exatamente o que o código já diz e não agrega contexto algum.
// Isso acaba só poluindo a leitura.


// ❌ Exemplo ruim:

// calcula desconto
function aplicarDesconto(valor, desconto) {
    // subtrai o desconto do valor
    return valor - desconto;
}



// ❌ Exemplo ruim:
// pega o preço
let a = produto.preco;

// pega a quantidade
let b = pedido.qtd;

// multiplica os dois
let c = a * b;

// mostra o total
console.log("Total:", c);
